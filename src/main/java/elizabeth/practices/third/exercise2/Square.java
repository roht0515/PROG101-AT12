package elizabeth.practices.third.exercise2;

public class Square extends Rectangle {
    public Square(final String tag, final String typeFigure, final int side) {
        super(tag, typeFigure, side, side);
    }
}
